{
  "key": "cloudflare",
  "name": "Cloudflare API Token",
  "service": "api.cloudflare.com",
  "credential_type": "generic",
  "auth_type": "api_key",
  "header": "Authorization: Bearer",
  "allowed_url_pattern": "https://api.cloudflare.com/*",
  "fields": [
    {
      "key": "secret_value",
      "label": "API Token",
      "placeholder": "Paste your Cloudflare API token",
      "required": true,
      "secret": true
    }
  ],
  "description": "Cloudflare API token. Injected as Authorization: Bearer header.",
  "tags": [
    "infrastructure",
    "cloudflare"
  ],
  "sort_order": 180
}
