{
  "key": "aws",
  "name": "AWS Credentials",
  "service": "aws",
  "credential_type": "aws",
  "auth_type": "aws_sigv4",
  "header": "Authorization: AWS4-HMAC-SHA256",
  "allowed_url_pattern": "https://**.amazonaws.com/*",
  "fields": [
    {
      "key": "aws_access_key_id",
      "label": "Access Key ID",
      "placeholder": "AKIAIOSFODNN7EXAMPLE",
      "required": true,
      "mono": true
    },
    {
      "key": "aws_secret_access_key",
      "label": "Secret Access Key",
      "placeholder": "Paste your secret access key",
      "required": true,
      "secret": true
    },
    {
      "key": "aws_region",
      "label": "Region",
      "placeholder": "us-east-1",
      "help": "Default AWS region for SigV4 signing. Can be overridden per request.",
      "mono": true
    },
    {
      "key": "aws_service",
      "label": "AWS service",
      "placeholder": "s3",
      "help": "The AWS service the signature is scoped to (e.g. s3, execute-api). Not the credential's own Service above. Optional.",
      "mono": true
    }
  ],
  "description": "AWS access credentials for SigV4 request signing. Supports any AWS service.",
  "tags": ["aws", "cloud"],
  "sort_order": 40
}
